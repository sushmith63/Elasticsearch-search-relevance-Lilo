{
  "query": {
    "bool": {
      "must": [
        {
          "multi_match": {
            "query": "__QUERY__",
            "type": "best_fields",
            "fields": ["title^10", "description^2", "category.text^1"],
            "operator": "or",
            "minimum_should_match": "2<75%",
            "fuzziness": "AUTO"
          }
        }
      ],
      "must_not": [
        { "prefix": { "category": "Makeup" } },
        { "prefix": { "category": "Cosmetics" } }
      ],
      "should": [
        { "match": { "title": { "query": "__QUERY__", "boost": 3 } } }
      ]
    }
  },
  "size": 10
}
